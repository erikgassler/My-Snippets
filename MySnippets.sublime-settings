{
	// Setup other folders that contain code snippets you want to include in your library
	// Each folder setting must have a "display" and "path" name|value pair
	// If either display or path is empty, or if path is not an accessible directory, then that setting will be skipped
	"folders":[
		{
			"display": "Other",
			"path": "C:/code library/"
		}
	]
}
